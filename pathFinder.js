'use strict';

class PathFinder {
  constructor (graph) {
    this.visited = {};
    this.unvisited = [];

    this.graph = graph;
  }

  findShortestPath (startNode, endNode) {

  }
}

module.exports = PathFinder;
